<h1>Created Events</h1>

<ul>
  <% @user_created_events.each do |event| %>
    <li>
      <%= event.title %><br>
      <%= event.body %><br>
      <%= event.date %><br>
      <%= event.location %>
    </li>
  <% end %>
</ul>
